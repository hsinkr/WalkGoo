const id=new URLSearchParams(location.search).get('id');const p=PLACES.find(x=>x.id===id)||JSON.parse(sessionStorage.getItem('walkgoo_last_detail')||'null');
function render(){const el=document.getElementById('detail');if(!p){el.innerHTML='<h1>상세 정보를 찾을 수 없습니다.</h1>';return}el.innerHTML=`<div class="detail-hero">${p.image?`<img src="${p.image}" alt="${p.title}">`:''}</div><h1>${p.title}</h1><p class="muted">${p.region}</p><p>${p.description||p.summary}</p><div class="meta"><b>${p.difficulty}</b><b>${p.duration}</b><b>${p.distance}</b><b>주차 ${p.parking}</b><b>화장실 ${p.toilet}</b></div><h2>추천 포인트</h2><ul class="list">${(p.points||[]).map(x=>`<li>${x}</li>`).join('')}</ul><h2>주의사항</h2><ul class="list">${(p.cautions||[]).map(x=>`<li>${x}</li>`).join('')}</ul><button class="btn fav-btn" data-id="${p.id}">${isFav(p.id)?'★ 저장됨':'☆ 즐겨찾기'}</button>`;document.querySelector('.fav-btn').onclick=()=>{toggleFav(p.id);render()};renderMap()}
function renderMap(){const help=document.getElementById('mapHelp'),box=document.getElementById('map');if(!p.lat||!p.lng){box.textContent='좌표 정보가 없습니다.';return}if(!CFG.KAKAO_JS_KEY){box.innerHTML=`<a class="btn primary" target="_blank" href="https://map.kakao.com/link/map/${encodeURIComponent(p.title)},${p.lat},${p.lng}">카카오맵에서 보기</a>`;help.textContent='config.sample.js에 KAKAO_JS_KEY를 입력하면 페이지 안에 지도가 표시됩니다.';return}const s=document.createElement('script');s.src=`https://dapi.kakao.com/v2/maps/sdk.js?appkey=${CFG.KAKAO_JS_KEY}&autoload=false`;s.onload=()=>kakao.maps.load(()=>{const pos=new kakao.maps.LatLng(p.lat,p.lng);const map=new kakao.maps.Map(box,{center:pos,level:4});new kakao.maps.Marker({map,position:pos,title:p.title});});document.head.appendChild(s)}render();
